<ui:FluentWindow
    x:Class="LiveCaptionsTranslator.BatchTranslationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:local="clr-namespace:LiveCaptionsTranslator"
    xmlns:utils="clr-namespace:LiveCaptionsTranslator.Utils"
    Title="一括翻訳結果"
    Width="800"
    Height="600"
    MinWidth="450"
    MinHeight="300"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <ui:FluentWindow.Resources>
        <utils:ValueToThicknessConverter x:Key="ValueToThicknessConverter" />
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Height="32"
            Title="一括翻訳結果"
            ShowMaximize="True"
            ShowMinimize="True">
            <ui:TitleBar.Header>
                <ui:TextBlock Padding="15,0,0,0" VerticalAlignment="Center" Text="一括翻訳結果" />
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Status / Header info -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <ui:TextBlock Text="API: " FontWeight="Bold" VerticalAlignment="Center" />
                <ui:TextBlock x:Name="ApiNameTextBlock" Text="OpenAI" Margin="4,0,24,0" VerticalAlignment="Center" />
                <ui:TextBlock Text="翻訳先: " FontWeight="Bold" VerticalAlignment="Center" />
                <ui:TextBlock x:Name="TargetLanguageTextBlock" Text="ja-JP" Margin="4,0,0,0" VerticalAlignment="Center" />
            </StackPanel>

            <Border Grid.Row="1" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
                <ListView x:Name="SentencesListView" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0" Background="Transparent">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="8,0"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource FillColorSecondaryBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="{Binding Source={x:Static local:Translator.Setting}, Path=BatchRowSpacing, Converter={StaticResource ValueToThicknessConverter}, ConverterParameter='0,1,0,1'}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                
                                <!-- Original Text (Left) -->
                                <ui:TextBlock Grid.Column="0" Text="{Binding SourceText}" TextWrapping="Wrap" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,12,0" LineHeight="20"/>
                                
                                <!-- Separator line -->
                                <Border Grid.Column="1" Width="1" Background="{DynamicResource CardStrokeColorDefaultBrush}" VerticalAlignment="Stretch" Margin="0,0"/>
                                
                                <!-- Translated Text (Right) -->
                                <ui:TextBlock Grid.Column="2" Text="{Binding TranslatedText}" TextWrapping="Wrap" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="12,0,0,0" LineHeight="20"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
